<div class="col-xs-2">
    <table class="table table-hover table-bordered">
      <thead>
        <tr>
          <th class="active text-center"><h5 class="genre_said">ジャンル検索</h5></th>
        </tr>
      </thead>
      <tbody>
        <td>
            <%= link_to "全商品を表示", items_path, class:"ec-genre-list-item" %>
        </td>
        <% genres.each do |genre| %>
          <tr>
            <!-- each文でジャンル名を取り出し、クリックした際にroutesで設定したitemsのパスを通って、itemsコントローラのsearchアクションに繋げてる。パラメーターにはクリックしたジャンルのidを渡してる。 -->
            <td class="underline"><i class="fas fa-caret-right"> <%= link_to genre.name, admin_genres_path(genre.id), method: :get %></i></td>
          </tr>
        <% end %>
      </tbody>
    </table>
</div>