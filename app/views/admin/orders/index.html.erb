<div class="container">
<h1>注文履歴一覧</h1>
<table class="table table-striped">
	<thead>
		<tr>
			<th>購入日時</th><th>購入者</th>
			<th>購入個数</th><th>注文ステータス</th>
		</tr>
	</thead>
	<body>
		<% @orders.each do |order| %>
			<tr>
				<td><%= order.created_at %></td>
				<td><%= order.name %></td>
				<td><%= %></td>
				<td><%= order.order_status %></td>
			</tr>
		<% end %>
	</body>
</table>
</div>